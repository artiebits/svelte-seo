language: node_js

node_js:
  - 16

cache:
  # cache both npm modules 
  directories:
    - ~/.npm
    - ~/.cache

script:
  - npm run format
  - npm run lint
  - npm run check
  - npx playwright install
  - npm run test
  - npm run build
