language: node_js

node_js:
  - 17

cache:
  # cache both npm modules 
  directories:
    - ~/.npm
    - ~/.cache

script:
  -  npm run format
  -  npm run lint
  -  npm run check
  - npm run test
  - npx playwright install
  - npm run build
